services:
  app:
    container_name: node-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /dockerfile/node_modules
    environment:
      - NODE_ENV=development
    command: npm start
